'use strict';

const Webpack = require('webpack');
const HtmlWebpackPlugin = require('html-webpack-plugin');
const Path = require('path');
const projectRoot = Path.join(__dirnam, '../');
const srcPath = path.join(projectRoot, 'src')
